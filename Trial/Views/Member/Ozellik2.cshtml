@model IEnumerable<Trial.Models.MarkaMember>

@{
    ViewBag.Title = "Ozellik2";
}

<h2>Ozellik2</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.markalar.MarkaAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.member.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.markalar.MarkaAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.member.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
