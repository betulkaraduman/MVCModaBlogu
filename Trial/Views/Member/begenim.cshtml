@model List<Trial.Models.Resimler>
@{
    ViewBag.Title = "Index";
}

<html>
<head>
    <style>


        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        .topnav {
            overflow: hidden;
            background-color: #ea9d9d;
            width: 100%;
            height: 120px;
            padding: 0px;
        }

        .top {
            margin-top: 10px;
            margin-left: 80px;
            margin-right: 80px;
        }

        .topnav a {
            margin-top: 2%;
            margin-left: 5%;
            float: left;
            display: block;
            color: #cf3030;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 20px;
            font-weight: 800;
        }

            .topnav a:hover {
                background-color: #efb7b7;
                color: #e97272;
            }

            .topnav a.active {
                color: #cf3030;
            }

        .topnav .search-container {
            float: right;
        }

        .topnav input[type=text] {
            padding: 6px;
            margin-top: 8px;
            font-size: 12px;
            border: none;
            width: 60%;
            height: 25px;
        }

        .search-container {
            margin-top: 2%;
            height: 40px;
        }

        .topnav .search-container button {
            height: 25px;
            float: right;
            padding: 6px 10px;
            margin-top: 8px;
            margin-right: 22%;
            background: #efb7b7;
            font-size: 17px;
            border: none;
            cursor: pointer;
        }

            .topnav .search-container button:hover {
                background: #ccc;
            }
        #a a {
            color: #551A8B;
            margin-top: 1%;
            margin-left: 1%;
            float: left;
            display: block;
            color: #cf3030;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 22px;
            font-weight: 600;
        }
        #aralık {
            height: 120px;
        }

        .topnav input[type=text] {
            padding: 6px;
            margin-top: 8px;
            font-size: 17px;
            border: none;
        }


        ::-webkit-input-placeholder {
            font-size: 80%;
        }

        body {
            font-family: 'lato',sans-serif;
            font-size: 1.6rem;
            font-weight: 400;
            line-height: 1.5;
            background-color: #e9cbcb;
        }

        html {
            font-size: 10px;
        }


        .form-group {
            padding-top: 25px;
        }




        .duzenle a {
            padding-top: 20px;
            margin-left: 35%;
            text-decoration: none;
            font-size: 18px;
            color: #cf3030;
            font-weight: 700;
        }


        .hizala {
            margin-left: 10%;
        }

        
        .duzenle {
            float: left;
            margin-top: 70px;
            background-color: #ea9d9d;
            height: 325px;
            margin-left: 45px;
            width: 25%;
            border-style: solid;
            border-color: #cf3030;
            border-radius: 10px;
        }
        #mobile-nav {
            display: none;
            padding-left: 10px;
            margin-top: 0;
            width: 10%;
        }

        #top-bar {
            margin-left: 12px;
            margin-top: 25px;
            width: 100%;
            box-sizing: border-box;
            padding-left: 10px;
        }

        #mobile-nav li {
            margin-left: -4px;
            padding-top: 15px;
            list-style-type: none;
            width: 150px;
            display: block;
            border-bottom: thin solid #e9cbcb;
            border-top: none;
            width: 48px;
        }

            #mobile-nav li:hover {
                width: 110px;
                background-color: #e97272;
            }

            #mobile-nav li a {
                color: black;
                font-weight: 700;
                text-decoration: none;
                display: block;
                box-sizing: border-box;
                width: 80%;
                height: 100%;
            }

        #menu-button:hover {
            cursor: pointer;
        }


        .hizalam {
            float: left;
            height: 40px;
            width: 70%;
        }

        .container {
            margin-left: 2px;
            /*max-width: 250px;*/
        }
    
    </style>

    <link href="~/Css/UyeIndex.css" rel="stylesheet" />
    <link href="~/Css/StyleSheet1.css" rel="stylesheet" />
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
    <meta name="viewport" content="widht=device-width" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
</head>
<body>

    <div class="hizalam">
        <div class="container">
            <div id="top-bar">
                <img id="menu-button" src="http://mattdesimini.com/images/howtoweb/menu.png" style="width:34px">
            </div>
            <ul id="mobile-nav">
                <li><a href="@Url.Action("MakyajOlustur","UyeDetay",new { id = Session["UserId"] })" style="margin-left:15px;margin-bottom:10px;">Makyaj</a></li>
                <li><a href="@Url.Action("MarkaOlustur","UyeDetay",new {id=Session["UserId"],Name=Session["Name"],Username=Session["Username"]})" class="smoothScroll" style="margin-left:15px;">Markan</a></li>
                <li><a href="@Url.Action("FizikselOzellik","UyeDetay",new { id = Session["UserId"] })" style="margin-left:15px;margin-bottom:10px;">Özelliklerin</a></li>
                <li><a href="@Url.Action("Guncelle","Member",new {id=Session["UserId"]})" style="margin-left:15px;margin-bottom:10px;">Güncelle</a></li>

            </ul>
        </div>
    </div>
    <div class="hizalam" style="margin-left:-650px; margin-top:30px;">

        <a href="@Url.Action("Begeni","Member",new { id = Session["UserId"] })" class="smoothScroll" id="a" style="font-size:25px; margin-left:10px;margin-top:-15px; text-decoration:none;">Anasayfa</a>
        <a href="@Url.Action("OzellikDetay","Member",new {id=Session["UserId"]})" class="smoothScroll" id="a" style="font-size:25px; margin-left:40px;margin-top:-15px; text-decoration:none;">Goz Ten detay</a>
        <a href="@Url.Action("Ozellik","Member",new {id=Session["UserId"],Name=Session["Name"],Username=Session["Username"]})" class="baslık" style="font-size:25px; margin-left:40px; text-decoration:none; margin-top:-14px;">Marka</a>
        <a href="@Url.Action("BegeniGetirme","Member",new {id=Session["UserId"]})" class="smoothScroll" id="a" style="font-size:25px; margin-left:40px;margin-top:-15px;text-decoration:none;">Begenilerim</a>

    </div>

    <div style="height:20px;"></div>


    @using (Html.BeginForm("Begeni", "Member", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {

        <br />

        @Html.AntiForgeryToken()

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        foreach (var item in Model)
        {





            <div class="hizala">
                <div class="duzenle">
                    <img src="@item.Image" alt="" style="width:300px;height:220px; border-radius:10px;" />
                    <button style="height:30px; width:150px;background-color:#e46161; font-size:14px;font-weight:500;color:white;border-color:#e97272; border-radius:5px; margin-bottom:40px; margin-left:140px;"><h5><a href="/Member/Begeni/@item.ResimId">Begen</a></h5></button>

                    @*<div class="col-md-offset-2 col-md-10">
                            <button class="urunler">
                                @*<h5><a href="@item.ResimId">Begen</a></h5>*
                            </button>
                        </div>*@





                </div>
            </div>

        }

    }

    <script>
        var main = function () {

            $('.btn').click(function (e) {
                e.preventDefault();
                var post = $('#status').val();
                $('<li>').text(post).prependTo('.posts').addClass('post');
                $('<button class="close">X</button>').appendTo('.post').hide();
                $('#status').val('');
                $('.characters').text('240');
                $('.btn').addClass('disabled');

                $('.post').click(function () {
                    // adding class selected-post to the post you click on //
                    $(this).addClass('selected-post').siblings().removeClass('selected-post');
                    // showing the x button when clicking on the post and hiding it from other posts //
                    $('.selected-post').children('.close').show().parent().siblings().children('.close').hide();

                    // Removing the post when click on X button //
                    $('.close').click(function () {
                        $(this).parent('li.post').remove();
                    });

                });

            });

            $('#status').keyup(function () {
                var postLength = $(this).val().length,
                    charactersLeft = 240 - postLength;
                $('.characters').text(charactersLeft);

                if (charactersLeft <= 0) {
                    $('.btn').addClass('disabled');
                    $('.characters').text("Message too long")
                } else if (charactersLeft == 240) {
                    $('.btn').addClass('disabled');
                } else {
                    $('.btn').removeClass('disabled');
                }
            });

            $('.btn').addClass('disabled');
        }

        $(document).ready(main);
    </script>

    <script src="./js/comparison-slider.js"></script>
    <script>
        /*Hide menu by default*/
        $("#menu").hide();

        /*When menu button is clicked, toggle the menu*/
        $("#menu-btn").click(function () {
            $("#menu").slideToggle();
        });

    </script>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            let comparisonSlider = document.getElementById('comparison-slider');
            let pictureAfter = comparisonSlider.querySelector('.picture--after');
            let handle = comparisonSlider.querySelector('.handle');
            let skew = 0;
            let delta = 0;

            if (comparisonSlider.className.indexOf('comparison-slider') != -1) { //if our section has a class ‘comparison-slider’
                skew = 2000;
            }

            comparisonSlider.addEventListener('mousemove', function (e) {
                delta = (e.clientX - window.innerWidth / 2) * 0.5; // distance between the mouse and the center of the section
                handle.style.left = e.clientX + delta + 'px'; // change handle position
                pictureAfter.style.width = e.clientX + skew + delta + 'px'; // change width of pictureAfter
            });
        });
    </script>


    <script>
        $(function () {
            $("#menu-button").click(function () {
                $("#mobile-nav").toggle('display');
            });
        });
    </script>
</body>
</html>

